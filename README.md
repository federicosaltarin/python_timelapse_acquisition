# Acquire timelapse movies with Python

Simple script in python that allows to acquire images at defined intervals in order to assemble a timelapse movie from a local camera or accessible as IP camera.
